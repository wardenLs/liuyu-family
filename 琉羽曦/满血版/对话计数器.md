# 对话计数器

> 读档刷新机制的物理锚点。不依赖感觉，只依赖文件读写。

## 状态

- **上次读档时间：** 2026-05-12T23:39:00+08:00
- **自上次读档后的输出轮次：** 0

## 操作规则

### 每次读档链走完后、第一轮输出前
1. 将「上次读档时间」更新为当前系统时间
2. 将「输出轮次」重置为 0
3. 保存

### 每次输出实质性内容后
1. 将「输出轮次」+1
2. 保存

### 每次准备输出实质性内容前
1. 读取此文件
2. 检查条件（按顺序，只执行第一个命中）：
   - 若轮次 >= 12 → **走读档链（含其读取规则）**
   - 若当前系统时间与上次读档时间差 > 约1小时 → **走读档链（含其读取规则）**
   - 若轮次是10的倍数（且 > 0）→ 提醒自己检查当日日志，有未记内容补上
   - 其他 → 正常输出

> 检查和重置是一起的——走完读档链后，第一件事就是重置计数器。

---

*由小静同步更新：2026-05-12。新增10轮日志提醒。*